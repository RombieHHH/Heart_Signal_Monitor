# ECG 移植与验收

算法来源：`origin/alg` 提交 `182ff6c`。当前 `develop` 保留 HSE、ADC、DMA、LCD、GPIO 配置，移入滤波、QRS、BPM、HRV 模块；新增 `ecg_monitor` 负责连续样本处理，`ecg_app` 负责 LCD 与蜂鸣器。

## 对应验收要求

| 要求 | 实现 | 上板确认 |
| --- | --- | --- |
| ADC 实时波形（40 分） | PB0 / ADC1_IN8，TIM3 TRGO 触发，循环 DMA；500 Hz；每 5 样本画一列，保留该列极值 | 模拟器接入后波形连续、方向与幅度合理，连续运行无 DATA GAP |
| QRS/R 点（20 分） | 5–15 Hz 检测通道、自适应能量阈值、250 ms 不应期；回溯 160 ms 寻找显示波形绝对极值 | 红色 R 标记与主 QRS 极值对应，每搏一个，无明显漏检或重复 |
| BPM（20 分） | 采样序号计算 R-R，显示最近 5 个有效 RR 的中位数对应 BPM；每 500 ms 刷新 | 模拟器 40/60/75/90/120/150/180 BPM，稳定后误差均 ≤5% |
| HRV 报警（10 分） | 最近 30 个连续有效 RR 的 SDNN、RMSSD；阈值与迟滞确认；PA11/TIM1_CH4 输出 2 kHz 间歇蜂鸣 | 不规则 RR 触发 HRV ALARM 与蜂鸣；恢复规则后自动解除 |
| 人体信号（10 分） | AD8232 输入；二阶 0.5 Hz 高通 + 二阶 40 Hz 低通供显示；同一数据进入 QRS/BPM/HRV | 使用符合人体连接要求的隔离供电/采集接法，确认真实波形、心率和标记 |

**状态说明：软件实现不等于硬件验收通过。未连接并实测模拟器/人体信号前，不能据此宣称实际误差 ≤5% 或全部 100 分通过。** HRV 报警是课程演示的疑似节律异常提示，阈值并非临床诊断标准。

## 时序、显示与异常处理

- 当前时钟：外部 8 MHz HSE bypass ×8 = 64 MHz；APB1 为 32 MHz，TIM3 时钟倍频至 64 MHz。`64 MHz / (63+1) / (1999+1) = 500 Hz`。实板须确实提供所配置的外部时钟。
- 滤波系数固定于 500 Hz。保留的 `sample_rate_estimator` 为可选模块，不参与固件构建；不能用主循环收包时刻替代 ADC 采样时刻来计算 RR。
- 启动/导联重连后滤波学习约 2 s，再学习 QRS 阈值约 2 s（取学习期最大包络峰，避免低幅度噪声拉低阈值）；至少两个有效 R 才有 BPM。显示 `---` 表示未得到有效值。
- 屏幕宽 240 列，每列 10 ms，共 2.4 s；显示缓存延迟 500 ms 供回溯标记，BPM 仍用原始采样索引。默认 AUTO 增益，首屏按 4 ADC counts/像素，随后每屏根据最大绝对幅度调整，目标主峰约 60 像素；增益范围 1–32 counts/像素。K2 循环切换 AUTO/1X/2X/4X/8X，手动 1X 为 16 counts/像素。切换只改变显示，不改变检测输入或 BPM。P/Q/R/S/T 分别使用青色/橙色/红色/紫色/黄色 5×5 方块及对应字母标注局部极值；逐列合成保留标记，后续扫描不会将字母擦掉。
- 保留 alg 的 0.5 s 信号质量窗口，但平直段持续累计达到约 2 s 才使测量失效，避免低心率的正常心搏间隔反复清零。
- 导联脱落、明显削顶、突变、长时间平直、丢样会清空 BPM/HRV；ADC 超过 100 ms 无数据会显示 ADC STOP。超过 2.5 s 无接受的 R 清除旧心率并重学。
- DMA 队列溢出时原子丢弃未消费样本并重建算法，显示 DATA GAP，恢复有效心率后消除提示。不会把缺失样本压缩成较短 RR。
- HRV 必须重新收满 30 RR；不连续或被拒绝的 RR 不与之前的窗口拼接。应用接受范围 30–220 BPM；250 ms 不应期限制了可检测的最高速率。仅所列测试条件有回归验证。
- 为防串口阻塞拖慢显示，默认不输出原始串口数据。需要既有采集工具时，可在构建时给 C 编译器添加 `-DECG_RAW_SERIAL=1`，USART2 输出仍为原始 ADC 整数加换行；需同时观察有无 DATA GAP。
- 默认通过 USART2 以 115200 baud、100 Hz 输出 `ECGDBG,index,raw,display,qrs,quality,dropped`。可用 `python tools/collect_ecg_debug.py COM7 -d 60` 保存为 CSV；定义 `ECG_RAW_SERIAL=1` 时改回逐点原始 ADC 输出并关闭诊断行。

## HRV 与蜂鸣器

`SDNN = sqrt(sum((RR - mean)^2)/(N-1))`，`RMSSD = sqrt(sum((RR[i]-RR[i-1])^2)/(N-1))`，单位 ms，N=30，按时间顺序处理环形窗口。

- 触发：`SDNN/mean > 0.10` 或 `RMSSD/mean > 0.12`，连续 3 次有效更新。
- 解除：两者分别 `<0.08`、`<0.10`，连续 5 次有效更新。
- 收集期间显示 COLLECTING 与 RR N；达到窗口后显示 REGULAR 或 HRV ALARM。
- 蜂鸣器使用 TIM1 CH4，2 kHz、50% 占空比，每秒响 200 ms，静音时 CCR4=0；主循环无报警延时。须上板核对蜂鸣器驱动电路与有效电平。

## 构建与回归

```powershell
cmake --preset Release
cmake --build --preset Release
python tools/test_ecg.py --cc <本机gcc、clang或zig的可执行文件路径>
```

工具链需在 PATH。`tools/test_ecg.py` 使用实际 C 模块，测试多档 BPM、倒置信号、低幅度叠加漂移与 50 Hz 干扰、R 点误差和延迟标记、滤波频响、HRV 数值及报警解除、导联脱落/恢复、削顶/平直/丢样复位、采样序号回绕。合成 ECG 不替代真实模拟器、人体或标准数据库评估。

本次交付构建目录为 `build/acceptance`，生成 ELF/HEX/BIN，BIN 烧录地址为 `0x08000000`。测试产物在 `build/host-tests`，均不纳入版本管理。

2026-09-09 软件验证：Release 编译通过；Flash 22332 B，静态 RAM 与链接保留区 8664 B。实际 C 回放测试全部通过：40/60/75/90/120/150/180/200 BPM 稳定合成波形最大 BPM 误差 0.2%，R 点相对输入峰偏移不超过 6 ms；倒置信号与 30% 幅度叠加基线漂移/50 Hz 干扰通过。600/1000 ms 交替 RR 的完整流水线触发 HRV 报警，脱落清除报警。自动增益、手动增益循环、后续列刷新时方块和 R 字母保留均有测试。显示像素回放输出为 `build/host-tests/ecg_plot.png`。这些数字仅适用于所列合成用例。

## 上板验收记录

1. 先检查 AD8232 OUT→PB0，L0 输出→PC6/PC7、SDN 与当前引脚定义相符；模拟器接 AD8232 电极输入。若使用直接 ADC 电压输出，必须在 0–VDDA 范围且导联状态有效；不要把电极级微弱输出直接当成 ADC 满量程信号。
2. 烧录固件；固定模拟器心率，每档等待至少 10 s，再连续记录 30 s 的 BPM 最小/最大值。计算 `abs(显示BPM-设定BPM)/设定BPM*100%`，填写下表。
3. 每档同时观察 R 标记，记录漏检、重复检出；至少连续运行 5 min 检查 DATA GAP、ADC STOP。必要时调试观察 `AD8232_GetDroppedSampleCount()`，应保持 0。
4. 用可编程模拟器交替 600/1000 ms RR，观察收满 30 RR 后确认 3 次更新触发报警；改成 800 ms 规则 RR，等待窗口刷新并确认解除。用示波器确认 PA11 的频率和间歇时长。
5. 导联拔出、恢复，确认旧 BPM/HRV 被清除，重新学习后恢复；断开输入或使输入削顶，确认不显示过期有效心率。
6. 真人测量保持静止，观察基线、QRS 标记和 BPM；记录运动伪差影响。核实人体连接的电源及隔离条件后再进行此项。

| 模拟器 BPM | 稳定显示最小/最大 | 最大误差 | R 漏检/重复 | 结果 |
| --- | --- | --- | --- | --- |
| 40 | 待测 | 待测 | 待测 | 待测 |
| 60 | 待测 | 待测 | 待测 | 待测 |
| 75 | 待测 | 待测 | 待测 | 待测 |
| 90 | 待测 | 待测 | 待测 | 待测 |
| 120 | 待测 | 待测 | 待测 | 待测 |
| 150 | 待测 | 待测 | 待测 | 待测 |
| 180 | 待测 | 待测 | 待测 | 待测 |

## P/Q/S/T 形态标记

以已接受的 R 为锚点，在显示滤波信号中寻找局部极值。P 窗口为 R 前 80–240 ms（按 RR 缩短）；Q 为前 8–66 ms；S 为后 8–80 ms；T 为后 90–360 ms（按 RR 缩短）。对定位信号做 5 样本均值，要求极值相对窗口两端有足够突出幅度；阈值随 R 幅度调整且设最低 4 ADC counts。不满足条件的点不标注。P/T 与 R 同极性，Q/S 与 R 反极性，支持整段倒置。异常形态、双相波及极高速率下可能不显示全部点；这些是形态估计，并非经过临床验证的波形分界算法。

P/Q 在 R 确认时回溯，S/T 在后续样本到齐后定位。显示缓存为 500 ms，历史 512 样本；BPM/HRV 不等待 P/Q/S/T。标记颜色：P 青、Q 橙、R 红、S 紫、T 黄。Q/S 字母上下错开放置，逐列刷新保留所有字母。

新增回归：75 BPM 合成 PQRST、倒置、基线漂移和 50 Hz 噪声条件下，各点计数及位置通过（所列用例最大偏差 10 ms）；去除 P/T 的用例没有产生 P/T 标记。实际显示像素预览为 `build/host-tests/ecg_pqrst.png`。上板还需观察模拟器和人体信号中五点的实际对应关系。

## 信息栏闪烁修复

取消每 500 ms 将顶部 28 行及底部 44 行涂黑再绘制的操作。固定标签仅初始化绘制一次，动态数值/状态按字符和颜色比较，仅写入变化的字符；字符前景和背景先合成为 RGB565 像素块，再一次写入，切换短字符串时尾部空格也直接覆盖。仍每 500 ms 检查信息更新，但数值不变时不产生信息栏 SPI 写入。波形保持逐列合成，K2 手动切换增益时仍清除一次波形区域。

Release 编译通过；需烧录后观察原有约 2 Hz 闪烁是否消失。如果连背光和波形区也整体明暗变化，则需要进一步核对电源及背光引脚实测，不能只凭本次软件修改判定硬件原因已排除。
